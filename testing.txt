cd build
sudo ./customkbd-daemon

# List connected keyboards
sudo ./customkbd list

# Select one by index (writes to configs/device.conf)
sudo ./customkbd select <index>

# Check status
sudo ./customkbd status


#####
sudo mount --bind $(pwd)/configs /etc/customkbd
sudo umount /etc/customkbd

# Run daemon normally
sudo ./src/customkbd-daemon
